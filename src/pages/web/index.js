import React from 'react';
import Layout from './Layout'

const index = () => {
    return (
        <Layout title="home" description = "React Boilerplate: Página de Inicio" keywords = "React.js, SEO Ready, Inicio">
           <h3>holas</h3>
        </Layout>
    );
};

export default index;